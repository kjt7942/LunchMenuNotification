# 작업 보고서: 식단 크롤링 로직 수정 (2026-04-21)

## 1. 📅 작업 개요
- **작업 내용**: 학교 홈페이지 구조 변경으로 인한 식단 데이터 추출 실패 해결
- **문제 현상**: 매일 아침 발송되는 식단 알림 메일이 도착하지 않음 (스크래핑 실패)
- **대상**: `SchoolFoodMenuRepository` (src/domain/diet/repo/school-repo.ts)

## 2. 🔍 원인 분석
- 학교 홈페이지(문경여중)의 HTML 구조가 변경되었습니다.
- 기존 코드에서 사용하던 선택자(`table.calendar`, `em`, `ul`)가 더 이상 유효하지 않거나 데이터가 포함된 태그와 일치하지 않았습니다.
- 특히 메뉴 데이터가 `ul > li` 구조에서 `p` 태그 내의 `br` 구분자로 변경되었습니다.

## 3. 🛠️ 해결 방법
- **선택자 최신화**:
    - 테이블: `table` (클래스 기반에서 태그 기반으로 유연하게 변경)
    - 날짜: `em` 또는 `.num` 클래스 모두 대응
    - 칼로리: 'Kcal' 텍스트를 포함하는 `p` 태그 탐색
    - 메뉴: `p` 태그 내의 `br` 태그를 분리하여 추출하도록 로직 보강
- **데이터 검증**: 로컬 테스트(`tests/test-repo.ts`)를 통해 2026.04.21(오늘) 식단 데이터를 성공적으로 가져오는 것을 확인하였습니다.

## 4. ✅ 결과 확인
- `getTodayMenu()` 호출 시 오늘 날짜(`2026.04.21`)의 메뉴와 칼로리를 정상적으로 반환합니다.
- GitHub Actions를 통한 다음 스케줄(07:45 KST)부터 정상적으로 발송될 예정입니다.

## 5. 💡 향후 고려 사항
- 홈페이지 구조가 다시 변경될 경우를 대비해, 데이터 추출 실패 시 경고 알림(Error Notification)을 보내는 기능을 추가하는 것을 권장합니다.
